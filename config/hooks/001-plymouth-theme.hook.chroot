#!/bin/sh
set -e

if [ -e /usr/sbin/plymouth-set-default-theme ] && [ -e /usr/share/plymouth/themes/tanglu-logo/tanglu-logo.plymouth ]
then
	plymouth-set-default-theme tanglu-logo
	/usr/sbin/update-initramfs -u
fi
